# work-20260828 — универсальный плагин: фикс дрейфа + вопрос об адаптерах

Started at: 2026-08-28 04:10 (UTC+3), источник: ручные часы; активное время: не контролировал.
Initial estimate: 15 / 40 active minutes.

## Запрос

Agent plugin 1.0 — универсальная спека для всех харнессов; отдельные адаптеры
больше не нужны — в этом соль. Проверка показала: корневой универсальный
plugin.json существовал, но отстал (0.3.0 против проекций 1.0.0) — дрейф от
вчерашнего бампа.

## Сделано сейчас

- root plugin.json -> 1.0.0 (единый источник версии/метаданных).
- Валидатор плагина: check_version_parity (root + .codex + .claude) —
  негативный тест подтверждает ловлю дрейфа.

## Открытый вопрос (два маршрута)

A) Инкрементально: команды (/secret,/askhuman) едут в плагин commands/ для
   всех харнессов; адаптеры помечаются deprecated, из них выносится
   переносимая семантика делегирования в один скилл; удаление следующим
   циклом.
B) Полная консолидация сейчас: adapters/ (5 каталогов, 288 строк +
   adapter.yaml + manifest + проверки) удаляются; семантика
   wait/join/hermes-middleware/zcode-профилей переезжает в переносимый
   скилл worker-dispatch внутри плагина; валидаторы/роллаут-манифест
   переписываются.

## Результат

- (заполняется при закрытии)
