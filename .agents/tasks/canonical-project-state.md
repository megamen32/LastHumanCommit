# Единый LHC и состояние целевого проекта

Started at 2026-09-09T14:47:55+03:00 (clock tool; active ledger canonical-project-state.active.json)

Результат: один канонический LHC, прозрачная история публикации; задания и ToDo
пишутся в целевой проект, а не в открытую папку другого проекта.
Канарейка: из сессии GPTAdmin выполнять изменение LHC и держать его ToDo в LHC;
установленный проектный router читает актуальный общий пакет.
Срез: аудит refs/источников, принадлежность проекту по текущей работе,
замена устаревшего LHC-блока GPTAdmin на общую ссылку без копирования правил.
Отброшено: разработка GPTAdmin, автоматическое слияние старых веток, ремонт 44.

Листья и исходные оценки: история 3–8 мин (Worker, gpt-5.6-luna);
маршрутизация и инструкции 8–18 мин (Lead, параллельно истории);
проверка интеграции 2–5 мин (после первых двух); публикация 2–5 мин.
Сумма 15–36 мин; две параллельные ветви, затем два последовательных шага:
прогноз elapsed max(3–8,8–18)+2–5+2–5 = 12–28 мин.

Найдено: main == origin/main == bfe68f7. В GPTAdmin встроен старый router,
который читает legacy current -> versions/d43342b. Код guard выбирает проект
по cwd, а не по явно выбранному проекту работы.

Уточнение владельца: никакого явного выбора или реестра. Lead должен понимать
контекст и использовать cwd/абсолютные пути целевого проекта. Пробный
project-bind и тесты удалены до публикации; изменения guard не входят в релиз.
Это исправление самой ошибки workflow: механизация обычного суждения дороже
его применения. Цель LHC — минимум токенов до полезного проверенного результата.

Аудит Worker (gpt-5.6-luna): main/origin 0/0; две отдельные архивные snapshots
85d2837/617578a не содержат потерянных canonical изменений. Остальные local refs
входят в main. d43342b — старый предок main, deployment drift, не потеря истории.

Ponytail скачан в `.tmp/ponytail-upstream`, commit
356918eba965ee1eac64bd3a7f0dd02108350de5. Адаптированы YAGNI, ladder и учёт
реальных ограничений в существующей задаче/комментарии; без второго плагина,
базы, переключателей и обязательных аудитов. См. docs/ponytail-adoption.md.

Проверка: общий validator и plugin parity PASS, независимый Reviewer APPROVE.
В GPTAdmin локально обновлены только managed router blocks AGENTS/CLAUDE;
специфичные Single-history completion и Reality-first testing сохранены снаружи.
Эти локальные изменения конфигурации не публикуют и не завершают приостановленную
продуктовую работу GPTAdmin; его четыре прежних незавершённых файла сохранены.
Версия общего плагина этого среза: 1.2.4.

Следующее явно запрошенное исследование: автоматическое Time Management на
hooks в ZCode/Codex/OpenCode. Конец turn не считать завершением бизнес-задачи;
проверить native события отдельно от переносимой спецификации пакета.
