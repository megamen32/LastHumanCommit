# Code profile

Load only for code changes.

- Prefer Strict Explict Types
- Exclipt Errors, но Ошибка это не повод уронить программу, ошибка это повод хорошенько ее отлогировать и нотифицировать оператора. Есть ошибки S0-уведомление напрямую, S1- в файл обязательный для чтения ИИ, 
- Opt-out INFO/DEBUG обязательны, желательно стуктурные логи чтобы было легко фильтровать. Обязательно ротация логов
- Не пиши код в /tmp пиши в .bin/ Скорее всего совсем скоро тебе придется Этот code перееспользовать. Тогд выносишь его в Постоянный: Два повтора повод задуматься, три повтора обязан.
- In Python, use f-strings instead of percent formatting.
- Split code files that exceed 800 lines unless they are generated, vendored, or
  constrained by an external format.
Все, что принадлежит проекту, должно быть в проекте. Systemd – часть проекта в деплое. Конфигурация, env – часть проекта. Logs – тоже часть проекта. Но что же делать канон OS тысячи путей? симлинки/rsync.Чтобы был и канон по os на месте, и все внутри проекта. Все внутри проекта обязательно должно быть. Достаточно открыть одну папку проекта и можно все увидеть/изменить/настроить, и больше не придется бегать по всем местам, чтобы искать конфигурации, логи, сервисы.

А ты точно уверен, что то, что ты сейчас хочешь написать, уже не написано сто раз и нужно просто подключить де факто std lib?

Ты точно уверен, что Google тебе не нужен?

Комментарии в коде может быть и не обязательны, но описание у каждой, даже приватной, функции обязательно. Входных/выходных данных, ошибок.

Если это можно решить функционально, решай функционально.(Нет внутрнего состояния, все зависит от входных аргументов) - Легко тестировать, воспроизводить, паралелизировать. 

Иначе - ООП. 

Сначала пишеться конкретный работающий класс, проверятеся что он работает и только потом делается абстракция, если она реально нужна. Иначе можно построить идеальный но вообще не нужный интефейс. 

YAGNI это прекрасно, пока все работает. Однако через год будет костыль для костыли, плюс революции в том что можно старые костыли выбросить, иногда надо просто начать снова с основы.

SH - плохой язык. no depency python - хороший.

То что я делаю будет ли работать на любой ОС? 