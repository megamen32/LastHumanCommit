# Code profile

Load only for code changes.

- Prefer Strict Explict Types
- Exclipt Errors
Ошибка это не повод уронить программу, ошибка это повод хорошенько ее отлогировать.
- Opt-out INFO/DEBUG обязательны, желательно стуктурные логи чтобы было легко фильтровать. Обязательно ротация логов
- Не пиши код в /tmp пиши в .bin/ Скорее всего совсем скоро тебе придется Этот code перееспользовать. Тогд выносишь его в Постоянный: Два повтора повод задуматься, три повтора обязан.
- In Python, use f-strings instead of percent formatting.
- Split code files that exceed 800 lines unless they are generated, vendored, or
  constrained by an external format.
Все, что принадлежит проекту, должно быть в проекте. Systemd – часть проекта в деплое. Инфигурация env – часть проекта. Logi – тоже часть проекта. Но что же делать? Винокси делает симлинки.Чтобы был и канон, и все внутри проекта. Все внутри проекта обязательно должно быть. Достаточно открыть эту папку, и больше не придется бегать по всем остальным местам, чтобы искать конфигурации, логи, сервисы.

А ты точно уверен, что то, что ты сейчас хочешь написать, уже не написано сто раз и нужно просто подключить де факто std lib?

Ты точно уверен, что Google тебе не нужен?

Комментарии может быть и не обязательны, но описание у каждой, даже приватной, функции обязательно. Входных, выходных и ошибок.

Если это можно решить функционально, решай функционально.

Если у этого есть состояние, решай ООП

Сначала конкретный класс и только потом абстракция, если она реально нужна.